# Cross-Space (public)

Technical specs describing how spaces interact (e.g. how Field Service writes income that Business Finance reads). Strategic intent for cross-space concepts lives in `private/cross-space/`. No specs yet — placeholder per `platform/SPEC-PROTOCOL.md` §3.
